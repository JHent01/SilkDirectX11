<Window x:Class="SilkDirectX11.Views.ReconectCamersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SilkDirectX11.Views"
        mc:Ignorable="d"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:mahx="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:enums="clr-namespace:SilkDirectX11.Enums"
        xmlns:convert="clr-namespace:SilkDirectX11.Converters"
        prism:ViewModelLocator.AutoWireViewModel="True"
         WindowStyle="None"
        ResizeMode="NoResize"
         AllowsTransparency="True"
        Title="Rec" Height="500" Width="700" Background="Transparent" Topmost="True" x:Name="Recon" mah:ControlsHelper.CornerRadius="15">
    <Border CornerRadius="25" BorderThickness="5"   Background="#252525">
        <Grid Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Content="{Binding Title, ElementName=Recon}"  HorizontalAlignment="Center" VerticalAlignment="Center"  Grid.Row="0" FontSize="30"/>
            <Label Content="Reconnecting to cameras, please wait..." HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,30,0,0" FontSize="26" Grid.Row="0"/>
            <Label Content="{Binding ReconectStatus}" HorizontalAlignment="Center" VerticalAlignment="Bottom"   FontSize="26" Grid.Row="1"/>
            <mah:MetroProgressBar IsIndeterminate="True" Width="300" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2" BorderBrush="#cccccc" BorderThickness="2"/>
        </Grid>
    </Border>
</Window>
