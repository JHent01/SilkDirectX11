<mahx:CustomDialog x:Class="SilkDirectX11.Views.AddCameraView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SilkDirectX11.Views"
        mc:Ignorable="d"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:mahx="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
         Background="Transparent">
    <!--Title="AddCameraVidew" Height="450" Width="800"-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>

        <Label Content="MainStreamStringConnect" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="15"/>
        <TextBox Grid.Column="1" Grid.Row="0" Grid.RowSpan="1" Text="{Binding MainSreamURL}"  HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="15" MinWidth="150"  Width="auto"/>

        <Label Content="SubStreamStringConnect" Grid.Row="1"    HorizontalAlignment="Left" VerticalAlignment="Top" Margin="15"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.RowSpan="1" Text="{Binding SubStreamURL }"   HorizontalAlignment="Left" VerticalAlignment="Top" Margin="15" MinWidth="150" Width="auto"/>

        <Label Content="Name Camera" Grid.Row="2"     HorizontalAlignment="Right" VerticalAlignment="Top" Margin="15" />
        <TextBox Grid.Row="2" Grid.Column="1" Grid.RowSpan="1" Text="{Binding CameraName}"  HorizontalAlignment="Left" VerticalAlignment="Top" Margin="15" MinWidth="100" Width="auto"/>

        <Button Content="Save" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="15" Width="100" Command="{Binding AddCameraCommand}" /> <!--CommandParameter=""-->

        
        <Button Content="Cansel"  Command="{Binding CanselCommand}" Width="100" Height="30"  Grid.Column="2" Grid.Row="3"/>
    </Grid>
</mahx:CustomDialog>
