<mahx:CustomDialog x:Class="SilkDirectX11.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SilkDirectX11.Views"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:mahx="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Height="450" Width="auto">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>
        <Label Content="Select Forder " Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
        <TextBox x:Name="Path" Text="{Binding SavePathSettings}"  HorizontalAlignment="Left" VerticalAlignment="Top" Margin="15" MinWidth="200"  Width="auto" Grid.Row="1" Grid.Column="1" IsReadOnly="True"/>
        <Button Content="Browse" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="15" Width="100" Command="{Binding SelectForderForCamers}"   />
        <Button Content="Save" Grid.Column="1" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" Width="100" Command="{Binding SaveSettingsCommand}" />
        <Button Content="Cansel" Command="{Binding CanselCommand}" Width="100"   Margin="10"  Grid.Column="1" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom"/> 

    </Grid>
</mahx:CustomDialog>
